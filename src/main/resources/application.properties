# Configuración de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/cdadatabase
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración de correo (sin cambios)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cda.project.software@gmail.com
spring.mail.password=sdvk rxas jbsc owau
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Puerto del servidor
server.port=8081

# Logging para depurar seguridad
logging.level.org.springframework.security=DEBUG
